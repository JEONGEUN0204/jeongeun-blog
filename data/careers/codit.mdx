---
companyId: codit
description: 국회 의안·법령·회의록·국회의원 데이터를 다루는 정책·입법 데이터 플랫폼 「Codit」과, 여기서 분리된 AI 챗봇 「ChatCODIT」의 웹·iOS·Android 앱을 담당했습니다. 대시보드 안정화부터 실시간 스트리밍·인앱결제·앱 배포까지 리드했습니다.
logo: /static/images/codit-logo-wide.png
logoSize:
  - 384
  - 93
logoFit: contain
tags:
  - React
  - Next.js
  - TypeScript
  - TanStack Query
  - Zustand
  - embla-carousel
  - Tailwind CSS
  - i18next
  - next-intl
  - Expo
  - React Native
  - reCAPTCHA Enterprise
  - Firebase App Check
  - EAS
  - Bitbucket
  - Jira
  - Figma
  - Confluence
  - Claude Code
---

<Section title="01. Codit 플랫폼 · 대시보드 (Web · 대표 성과)">

<Meta
  tech={'React · TanStack Query · Zustand · embla-carousel · TypeScript'}
  role={'대시보드 프론트엔드 데이터 로딩 아키텍처·쿼리 계층·캐러셀 인터랙션 설계 및 구현'}
/>

**대시보드 순차 로딩 아키텍처 설계**

<Steps
  problem={'진입 시 뉴스·의안·법령 등 전 섹션·전 슬라이드를 한 번에 요청해 초기 로딩이 느림'}
  improve={'현재 슬라이드만 요청하고 인접(±1) 슬라이드는 백그라운드 prefetch하도록 재설계'}
  result={'초기 요청 데이터량 축소, 최초 진입 로딩 개선'}
/>

**대시보드 쿼리 계층 단일화**

<Steps
  problem={'섹션마다 쿼리 훅이 산발적으로 흩어져 유지보수가 어려움'}
  improve={'useDashboard 단일 훅·단일 캐시 정책으로 통합, deviceType 자동 주입'}
  result={'신규 섹션 추가 비용 감소'}
/>

**캐러셀 인터랙션 고도화**

<Steps
  problem={'한 번 스와이프에 여러 칸이 넘어가고 섹션 간 동작이 불일치'}
  improve={'useSwipeOneStep 등 커스텀 훅으로 1칸 이동·드래그 스냅·화살표 네비 구현'}
  result={'스와이프 정확도·인터랙션 일관성 확보'}
/>

</Section>

<Section title="02. Codit 플랫폼 · 품질·구조 (Web)">

<Meta tech={'React · Tailwind CSS · i18next · TypeScript'} role={'AppShell 구조 도입 담당'} />

**AppShell 아키텍처 도입**

<Steps
  problem={'라우트 전환마다 전역 폴백 노출'}
  improve={'AppShell 분리·Suspense L1/L3 계층화'}
  result={'첫 진입 폴백 최소화'}
/>

</Section>

<ProjectNarrative id="codit-agents-md" no="03" />

<ProjectNarrative id="codit-tailwind-skill" no="04" />

<ProjectNarrative id="codit-chatcodit" no="05" suffix="Web · 핵심">

**연결 안정성 & 채팅 상태 단일화**

<Steps
  improve={
    'Promise.race 타임아웃으로 죽은 연결 감지·예외 방어, Zustand StreamingStore로 상태를 통합하고 이벤트 리듀서 패턴으로 불변 갱신'
  }
  result={'도착 순서 무관 일관성, 신규 답변 타입 확장 기반 확보'}
/>

</ProjectNarrative>

<Section title="06. ChatCODIT · 대화형 문서 초안 작성(Draft) (Web)">

<Meta
  tech={'Next.js · SSE · Zustand · TypeScript'}
  role={'멀티턴 문서 생성 플로우 설계·블록 모델 확장·에러 복원 로직·파일 첨부 파이프라인 구현 담당'}
/>

**기존 스트리밍 아키텍처 위 멀티턴 문서 생성 확장**

<Steps
  problem={
    '템플릿 질문에 순차로 답하면 이를 종합해 법률 문서 초안을 생성하는 "질문→답변→문서" 멀티턴 흐름을, 기존 스트리밍 구조를 깨지 않고 얹어야 함'
  }
  improve={
    '기존 SSE 스토어에 question·user_message·document 블록 타입만 추가해 기존 렌더링 경로 수정 없이 통합(확장 가능한 구조를 실제로 검증), 세션 판정 로직을 draftBlocks.utils로 단일 소스화'
  }
  result={'기존 아키텍처 재사용으로 신규 피처를 무중단 확장'}
/>

**에러 복원력 있는 UX**

<Steps
  improve={
    "SSE 에러를 StreamError로 타입화하고 API 에러 코드를 카탈로그화해 재시도가능/입력오류/치명 3갈래 분기. 중복 요청(409) 시 '생성 중' 표시를 유지한 채 Retry-After 대기→세션 폴링→같은 키로 replay하는 자기복구 구현"
  }
  result={'중복 요청·네트워크 오류에도 진행 상태를 잃지 않는 복원력 확보'}
/>

**파일 첨부 파이프라인**

- 초안 질문에 파일을 multipart로 전송하고, 첨부 개수·용량(5MB) 제한과 초과 시 안내, '내 문서에서 업로드' 개수 제한 연동을 처리. 기존 파일은 재업로드하지 않고 existingFileIds로 전달하는 방식으로 설계.

</Section>

<Section title="07. ChatCODIT · 렌더링·구축·운영 (Web)">

<Meta
  tech={'Next.js · React · TypeScript · next-intl · Tailwind CSS'}
  role={'반응형 웹 대응·0→1 웹 구축(React→Next.js 마이그레이션·회원·인증·SEO·다국어) 담당'}
/>

**반응형 웹 — 다층 전략 설계**

<Steps
  problem={
    '단순 CSS 분기로는 모바일 바텀시트 vs 데스크톱 모달·문서 패널 같은 구조적 차이를 감당하기 어려움'
  }
  improve={
    '① next/server userAgent로 SSR 시점 디바이스 확정(DeviceProvider·useDeviceType)해 hydration 깜빡임 방지 ② desktop/mobile 라우트 분리 + device rewrite ③ Tailwind @theme 커스텀 브레이크포인트 토큰화(1920~480px) ④ 컴포넌트 단위 반응형으로 답변 영역 콘텐츠를 화면 폭에 맞춰 동적 배치'
  }
  result={'감지→라우팅→토큰→컴포넌트 전 계층에서 일관된 반응형 확보'}
/>

**0→1 신규 구축 & 배포 단순화**

- React → Next.js(App Router) 마이그레이션, 로그인 Guard(회원·인증)·next-intl 다국어·SEO 구축. Bitbucket Pipeline으로 테스트 서버 배포 과정을 자동화·단순화.

</Section>

<Section title="08. ChatCODIT App · 구축·결제 (iOS · Android)">

<Meta
  tech={'Expo · React Native · TypeScript · Zustand · TanStack Query · expo-iap'}
  role={'웹→네이티브 마이그레이션·인앱결제(IAP)·구독 결제 신뢰성 설계 및 구현'}
/>

- Expo Router·스트리밍 채팅·마크다운 이식으로 1.0.0 정식 출시. expo-iap 기반 BASIC/PRO 구독을 서버 검증까지 연동하고 결제 상태를 IAPProvider로 통합.

**결제 신뢰성 확보**

<Steps
  problem={'인앱결제 검증 과정에서 동일 거래가 중복으로 verify되는 이슈 발생'}
  improve={'앱 실행 시 미완료 거래 sweep·재검증, 계정 UUID 바인딩·중복 구독 방지 가드 적용'}
  result={'중복 verify 해소'}
/>

</Section>

<Section title="09. ChatCODIT App · 인증·보안·배포 (iOS · Android)">

<Meta
  tech={'Expo · React Native · reCAPTCHA Enterprise · Firebase App Check · EAS'}
  role={'소셜 로그인·딥링크 처리·앱 보안·EAS 무중단 배포 담당'}
/>

- 이메일·Apple·Google 소셜 로그인을 구현하고, 비밀번호 재설정·이메일 인증 링크를 딥링크로 처리해 앱 설치 시 앱으로, 미설치 시 모바일 웹으로 연결되도록 구현. reCAPTCHA Enterprise 봇 방어 + Firebase App Check로 클라이언트 무결성 검증을 적용.
- EAS Build 프로파일(dev/staging/production) 분리, EAS Update(OTA)·rollback 스크립트로 무중단 배포·빠른 복구 체계 구축. 출시 이후 발생하는 버그와 운영 이슈를 지속적으로 대응하고, i18n 수정처럼 급한 변경은 OTA로 즉시 반영.

</Section>

<Section title="10. 더코딧 앱 (WebView 하이브리드) (iOS · Android · WebView)">

<Meta
  tech={
    'React Native · Expo · expo-router · react-native-webview · EAS · Firebase App Distribution'
  }
  role={'WebView 네비게이션·브릿지 트러블슈팅·릴리즈/스토어 배포 담당'}
/>

**원문링크 모달 2번 열림 — 브릿지 주입 타이밍 이슈 규명**

<Steps
  problem={
    '원문링크 클릭 시 모달이 두 번 열림. 근본 원인은 네이티브 판별 상수 isNativeMobile이 모듈 로드 시점에 1번만 평가되는데, 그 시점엔 injectedObjectJson이 아직 늦게 주입돼 값이 false로 캐싱된 것'
  }
  improve={
    '"ReactNativeWebView 객체의 존재 자체가 앱 웹뷰 환경"이라는 판단으로 판별 기준을 Boolean(window.ReactNativeWebView)로 변경해 주입 타이밍 의존을 제거'
  }
  result={'중복 오픈 제거, iOS·Android 동작 일관성 확보'}
/>

**배포 자동화 & 릴리즈 오너십**

- EAS staging 빌드와 Firebase App Distribution 배포 스크립트로 QA/내부 배포 파이프라인을 간소화하고, 여러 마이너 버전의 릴리즈와 스토어 배포 전 과정(브랜치 병합·실서버 반영)을 주도.

</Section>
